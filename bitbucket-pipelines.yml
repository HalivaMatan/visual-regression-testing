# A sample pipeline implementation
pipelines:
  pull-requests:
    "**":
      # Other steps in the pipeline
      # 👇 Adds Chromatic as a step in the pipeline
      - step:
          image: node:16
          name: "Publish to Chromatic"
          # Other pipeline configuration
          script:
            - npm install
              # 👇 Runs Chromatic with the flag to prevent pipeline failure
            - npm run chromatic
      - step:
          image: node:16
          name: "Publish to Chromatic2"
          # Other pipeline configuration
          script:
            - npm install
              # 👇 Runs Chromatic with the flag to prevent pipeline failure
            - percy storybook https://61e3533c1c3edc003a60e10c-heucocspox.chromatic.com/
